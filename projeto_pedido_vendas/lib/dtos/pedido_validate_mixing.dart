mixin PedidoValidate {
  void validateDataPedido(DateTime dataPedido) {
    // Adicione aqui a lógica de validação para a data do pedido, se necessário
  }

  void validateObservacao(String observacao) {
    // Adicione aqui a lógica de validação para a observação do pedido, se necessário
  }

  void validateFormaPagamento(String formaPagamento) {
    // Adicione aqui a lógica de validação para a forma de pagamento do pedido, se necessário
  }
}
